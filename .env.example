# 聊天機器人後端金鑰範本
# 這個檔案「不含真實金鑰」，可以安全進版控。
# 實際部署時：在 Vercel 專案 Settings → Environment Variables 逐一設定下列變數。
# 本機測試（vercel dev）時：複製成 .env.local 並填入真實金鑰（.env.local 已被 .gitignore 排除）。
#
# 至少設定一組即可運作；設定越多，fallback 越穩定。

# Groq（免費）: https://console.groq.com/keys
GROQ_API_KEY=

# Google Gemini（免費）: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# OpenRouter（免費模型）: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# 選用：僅在前端與 API 不同網域時才需要（逗號分隔）
# 例：ALLOWED_ORIGINS=https://chenyuhsu413.github.io
ALLOWED_ORIGINS=
